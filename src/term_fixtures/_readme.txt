the files in this director are *NOT* required at runtime.
They are *ONLY* for testing. They are exported with
`:erlang.term_to_binary/1`. See the _create_term_fixture.exs
script for generating the fixtures. 
